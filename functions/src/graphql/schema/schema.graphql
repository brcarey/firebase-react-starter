type User {
  firstName: String!
  lastName: String!
}

type GalleryItem {
  id: Int!
  imageUrl: String!
  heading: String!
  description: String!
}

type Query {
  me: User!
  gallery: [GalleryItem!]!
}

schema {
  query: Query
}
